#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    char s1[100],s2[100];
    scanf("%s%s",&s1,&s2);
    if(strcmp(s1,s2)==0){
        printf("Strings are same");
    }
    else{
        printf("Strings are not same");
    }
    return 0;
}
