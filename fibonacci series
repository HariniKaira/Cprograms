#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d",&n);
    int a=0,b=1;
    int sum=a+b;
    for(int i=3;i<=n;i++){
        a=b;
        b=sum;
        sum=a+b;
    }
    printf("%d",sum);
    return 0;
}
