#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int num;
    // int val=10;
    int res=0;
    int tot=0;
    scanf("%d",&num);
    for(int i=0;i<num;i++){
        res=2+((res*10));
        tot=tot+res;
    }printf("%d",tot);
      
    return 0;
}
