#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int d,m,y,s;
    scanf("%d%d%d%d",&d,&m,&y,&s);
    int sum=d+m+y;
    if(s>50 && s<100){
        if(sum%3==0 && sum%4==0){
            printf("Cinema Day");
        }
        else{
            printf("Not a Cinema Day");
        }
    }
    else{
        printf("Not a Cinema Day");
    }

    return 0;
}
