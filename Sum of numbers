#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d",&n);
    int sum=0;
    while(n!=-1){
        sum=sum+n;
        scanf("%d",&n);
        
    }
    printf("%d",sum);
    return 0;
}
