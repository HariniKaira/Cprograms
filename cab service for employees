#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,s,e,i;
    scanf("%d%d%d",&n,&s,&e);
    int a[n];
    for(i=0;i<n;i++){
        scanf("%d",&a[i]);
    }
    for(i=0;i<n;i++){
        if(a[i]>s && a[i]<e){
            printf("%d ",a[i]);
        }
    }
    return 0;
}
